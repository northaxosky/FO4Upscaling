[Settings]

; Enable frame generation
bFrameGenerationMode=true

; Enable frame rate limiting if VSync is disabled
bFrameLimitMode=true

; Frame generation backend (0=FSR3, 1=DLSS-G)
; DLSS-G requires NVIDIA RTX 40+ series GPU. Falls back to FSR3 if unsupported.
iFrameGenType=0